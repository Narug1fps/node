<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>node</title>
  </head>
  <body>
    <div class="container">
      <div class="content">
        <% id (users.lengsth > 0){ %> <% users.forEach(user => { %>

        <h3 class="title"><%= user.firstName %><%= user.lastName %></h3>
        <p><%= user.email %></p>
        <% }) %> <% } else{ %>
            <p>Não há usuários para exibir...</p>
            <% } %>
      </div>
    </div>
  </body>
</html>
